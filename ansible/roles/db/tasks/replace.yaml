---
- expect:
    command: /bin/bash -c 'mysql
               --host={{ db_params.host }}
               --port={{ db_params.port }}
               --user={{ db_params.user }}
               --password
      {{ db_params.name }} < {{ db_dump_file_path }}'
    timeout: 120
    responses:
      password: "{{ db_params.password }}"
