# {{ ansible_managed }}

<IfModule headers_module>
    Header always set X-Frame-Options "deny"
    Header always set Referrer-Policy "no-referrer, strict-origin-when-cross-origin"

    # If Symfony was misconfigured, and didn't send the cache headers, use this default
    Header setifempty Cache-Control "public, max-age=3655"
</IfModule>

<IfModule deflate_module>
    <IfModule !brotli_module>
        SetOutputFilter DEFLATE
        SetEnvIfNoCase Request_URI "\.(?:gif|jpe?g|png)$" no-gzip
    </IfModule>
</IfModule>

<IfModule brotli_module>
    SetOutputFilter BROTLI_COMPRESS
    SetEnvIfNoCase Request_URI "\.(?:gif|jpe?g|png)$" no-br
</IfModule>
