---
ansible_ssh_host: "158.69.0.250"
ansible_ssh_port: 33520
ansible_ssh_user: "rocky"

user_group: "rocky"

yarn_cache_dirpath: "{{ deployment_path }}/var/yarn-cache"
database_url: "sqlite:///%kernel.project_dir%/var/db.sqlite" # grep-code-database-path
database_path: "{{ deployment_path }}/var/db.sqlite" # grep-code-database-path

default_branch: "develop"
beta_branch: "beta"
prod_branch: "main"
