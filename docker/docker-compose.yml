###############################################################################
#                          Generated on phpdocker.io                          #
###############################################################################

version: "3.1"
services:
  webserver:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ..:/var/www/html
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8080:80"

  php-fpm:
    build: php-fpm
    restart: unless-stopped
    volumes:
      - ..:/var/www/html
