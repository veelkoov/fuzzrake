var ARTISANS;

$(document).ready(function () {
    ARTISANS = [
        {% for artisan in artisans %}
        new fuzzrake.Artisan({{ artisan.jsonArray|trim('[]')|raw }}),
        {% endfor %}
    ];

    fuzzrake.init();
});
