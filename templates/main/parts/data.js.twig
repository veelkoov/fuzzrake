{% cache 'parts_data.js' %}

var MAKER_IDS_MAP = null;

window.loadFuzzrakeData = function () { // grep-window-load-fuzzrake-data
    MAKER_IDS_MAP = {
        {% for old, new in makerIdsMap %}
            '{{ old }}': '{{ new }}',
        {% endfor %}
    };
}

{% endcache %}
