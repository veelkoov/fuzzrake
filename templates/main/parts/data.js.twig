{% cache 'parts_data.js' %}

var MAKER_IDS_MAP = null;

window.loadFuzzrakeData = function () {
    MAKER_IDS_MAP = {
        {% for old, new in makerIdsMap %}
            '{{ old }}': '{{ new }}',
        {% endfor %}
    };
}

{% endcache %}
