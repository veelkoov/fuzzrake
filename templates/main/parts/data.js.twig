{# TODO: Cache? #}

var TOTAL_ARTISANS_COUNT = {{ stats.totalArtisansCount }};
var FILTERS_OPTIONS = null;

window.loadFuzzrakeData = function () {
    FILTERS_OPTIONS = {{ filters|json_encode|raw }};
};
