var ARTISANS;

$(document).ready(function () {
    ARTISANS = [
        {% for artisan in artisans %}
        new fuzzrake.Artisan({{ artisan|json_encode|trim('[]')|raw }}),
        {% endfor %}
    ];

    fuzzrake.init();
});
