To reset your password, please visit the following link:

{{ url('rt_user_password_reset_form', {token: reset_token.token}) }}

This link will expire in {{ reset_token.expirationMessageKey|trans(reset_token.expirationMessageData, 'ResetPasswordBundle') }}.

In case of any issues please contact the site administration:
{{ url('rt_contact') }}
